import fabio
import sys
import matplotlib.pyplot as plt

def main(filelist):
#     for f in filelist[1:]:
#         mask1 = fabio.open(filelinst[0]).data
#         mask2 = fabio.open(f).data
#         newmask= mask1 +mask2
#         newmask[newmask!=0]=1         
   

    mask1 = fabio.open(filelinst[0]).data
    fig1 = plt.
    figure(figsize=(7,7), dpi=80)
    plt.imshow(mask1, vmin=0, vmax=1, cmap='gray')
    #plt.axis([0, 517, 0, 517])  
    plt.colorbar()
    plt.title("module mask")
    ax = plt.gca()
    ax.set_aspect('equal') 

    plt.draw()  

if __name__ == '__main__':
    if len(sys.argv) < 2:
        print 'usage: python integrate.py <at least 2 mask files .edf>'
        sys.exit(0)
    else:
        main(sys.argv[1:])

    
